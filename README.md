# Nuri

## Project Setup

The Xcode project has to be generated by using [Tuist](https://github.com/tuist/tuist). 

After installing Tuist, generate the project:

```
tuist generate
```

Then select the Nuri scheme and hit run.